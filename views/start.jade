extends layout

block content
    h1= title
    p Welcome to #{title}
    form(action="/upload", method="post")
        textarea(name="text", rows="1", cols="60")
        input(type="submit" value="Submit Text")